{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "build": "bun build --compile src/server.ts --outfile dist/server",
    "start": "./dist/server",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "dependencies": {
    "@fastify/cors": "^11",
    "@fastify/swagger": "^9.7.0",
    "@scalar/fastify-api-reference": "^1.52.1",
    "fastify": "^5",
    "fastify-type-provider-zod": "^6.1.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
